#include "GPhysics.h"

namespace rev {
//////////////////////////////////////////////////////////////////////////////////////////////////
PhysicsBase::PhysicsBase()
{
}
//////////////////////////////////////////////////////////////////////////////////////////////////
PhysicsBase::PhysicsBase(const GString & name) :
    Nameable(name)
{
}
//////////////////////////////////////////////////////////////////////////////////////////////////
PhysicsBase::~PhysicsBase()
{
}
//////////////////////////////////////////////////////////////////////////////////////////////////
QJsonValue PhysicsBase::asJson(const SerializationContext& context) const
{
    QJsonObject object;
    object.insert("name", m_name.c_str());

    return object;
}
//////////////////////////////////////////////////////////////////////////////////////////////////
void PhysicsBase::loadFromJson(const QJsonValue& json, const SerializationContext& context)
{
    Q_UNUSED(context)

    QJsonObject object = json.toObject();
    m_name = object.value("name").toString();
}


//////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////////////
}